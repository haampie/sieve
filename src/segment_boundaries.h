#ifndef __INCLUDE_SEGMENTBOUNDARIES__
#define __INCLUDE_SEGMENTBOUNDARIES__

#include "../config.h"

void segmentBoundaries(size_t *from, size_t *to, size_t rangeStart, size_t rangeEnd, processors P, processors index);

#endif